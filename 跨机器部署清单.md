# 跨机器部署清单

把 dev-expert 部署到**另一台**机器给 opencode 使用，按下表操作。

## 一、要拷什么

从本机 `C:\Users\13119\Desktop\dev-expert迁移opencode\`（或源目录）取两类东西：

| 来源 | 目标机器落点 | 说明 |
| --- | --- | --- |
| `C:\Users\13119\.claude\skills\dev-expert\`（整个目录，81 文件） | `~/.claude/skills/dev-expert/` | 技能本体，**整体拷，别漏 references/** |
| `dev-expert-bridge.js` | `~/.config/opencode/plugins/dev-expert-bridge.js` | 桥接插件 |

技能本体**完全不含任何本机绝对路径**（已全仓 grep 验证：SKILL.md、references/、hooks/、scripts/ 均无硬编码），拷过去即可用。

macOS / Linux 上路径为：
- 技能：`~/.claude/skills/dev-expert/`
- 插件：`~/.config/opencode/plugins/dev-expert-bridge.js`

## 二、目标机器必须有的东西

1. **opencode 本体**（没有它整个部署无意义）。
2. **Python ≥ 3.8**。插件按此顺序自动探测：
   - 环境变量 `DEV_EXPERT_PYTHON`（最可靠，建议直接设）
   - `~/.workbuddy/binaries/python/versions/*/python.exe`（扫版本号目录，不写死版本）
   - `~/.codebuddy/binaries/python/versions/*/...`
   - PATH 中的 `py` / `python3` / `python`

   **目标机器若没有 WorkBuddy，直接设环境变量最省事**：
   ```bash
   export DEV_EXPERT_PYTHON=/usr/bin/python3        # macOS / Linux
   setx DEV_EXPERT_PYTHON "C:\Python313\python.exe" # Windows
   ```

## 三、按需设的环境变量

| 变量 | 作用 | 默认值 |
| --- | --- | --- |
| `DEV_EXPERT_PYTHON` | Python 解释器绝对路径 | 自动探测 |
| `DEV_EXPERT_SKILL_DIR` | 技能根目录 | `~/.claude/skills/dev-expert` |
| `PROJECT_ROOT` | 项目根，用于 `.ai-memory` / 知识图谱定位 | 插件按 `worktree` → `directory` 自动注入 |

技能路径与默认值一致时，只需设 `DEV_EXPERT_PYTHON` 一个变量。

## 四、验证是否装好

在目标机器上依次检查：

```bash
# 1. 技能能被发现（opencode 内）
#    启动 opencode 后，看 skill 工具描述里是否列出 dev-expert

# 2. 插件语法与加载
node --check ~/.config/opencode/plugins/dev-expert-bridge.js

# 3. 钩子可用（直接喂 stdin，应 exit 2）
echo '{"tool_input":{"filePath":"/tmp/vendor/x.js"}}' | python3 ~/.claude/skills/dev-expert/hooks/guard_dirs.py; echo "exit=$?"

# 4. 全链路（把 verify_bridge.cjs 也拷过去后）
node verify_bridge.cjs    # 期望 8/8 通过
```

第 3 步期望 `exit=2` 并输出 `[GUARD-BLOCK]`；若输出为空或 exit 0，说明 Python 没跑起来，
改设 `DEV_EXPERT_PYTHON` 到真实解释器路径。

## 五、三个跨机器的坑

1. **`verify_bridge.cjs` 里的 Python 路径是写死的**。它只用于验证，拷到别的机器
   记得把文件顶部 `const PYTHON = "..."` 改成目标机器的解释器路径。

2. **`install_hooks.py` / `init_deploy.py` 会把路径固化进 JSON**（这是设计使然，
   "装一次写一次真实路径"）。所以：
   - **不要**把本机生成的 `hooks.installed.json` 拷到别的机器用，路径是错的。
   - 那套脚本面向的是 CodeBuddy / Trae / Cursor / Claude Code / Windsurf，
     **不含 opencode**，对 opencode 无用 —— opencode 一律走本插件。
   - 目标机器若同时用 Claude Code，可在那边重跑 `install_hooks.py` 生成对应配置。

3. **`build_graph.py` 的知识图谱 cache 不要拷**。它是按项目生成的运行时产物，
   换机器要重建；且若目标机器缺第三方依赖，需先建 venv。

## 六、最省事的做法

如果目标机器也是 Windows 且装了 WorkBuddy，那**除了目录名之外零改动**：
把技能目录拷到 `~/.claude/skills/dev-expert/`、插件拷到 `~/.config/opencode/plugins/`，
启动 opencode 即可，Python 探测会自动命中 `~/.workbuddy/binaries/python/`。

跨平台（macOS / Linux）则务必设一次 `DEV_EXPERT_PYTHON`。
